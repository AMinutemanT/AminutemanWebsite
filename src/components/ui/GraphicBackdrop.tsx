/** Neutral masthead surface for pages without documentary imagery. */
export function GraphicBackdrop() {
  return <div aria-hidden="true" className="absolute inset-0 bg-abyss" />;
}
export default GraphicBackdrop;
